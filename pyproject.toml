[tool.poetry]
name = "elibrary"
version = "0.1.0"
description = ""
authors = ["Your Name <you@example.com>"]
license = "MIT"
readme = "README.md"

[tool.poetry.scripts]
elibrary = "elibrary.main:run"
convert-xlsx-to-sql = "convert-xlsx-to-sql.main:run"

[tool.poetry.dependencies]
python = ">=3.10,<3.13"
flask = "^2.3.0"
flask-sqlalchemy = "^3.1.1"
flask-login = "^0.6.2"
werkzeug = "2.3.0"
flask-api = "^3.1"
flask-rangerequest = "^0.0.2"
pandas = "^2.1.1"
flask-jwt-extended = "^4.5.3"
flask-cors = "^4.0.0"
python-dotenv = "^1.0.0"


[tool.poetry.group.dev.dependencies]
black = "^23.9.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
